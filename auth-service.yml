server:
  port: 8082

application:
  security:
    jwt:
      secret-key: KoNBlgUHCn24kEKj0ubb/GQebGqQjqOPDhTr/hICxbQ=
      access-token-expiration: 15m
      refresh-token-expiration: 7d

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/streaming_auth
    username: streaming
    password: streaming123

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
