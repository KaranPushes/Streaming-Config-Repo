server:
  port: 8082

application:
  security:
    jwt:
      secret-key: KoNBlgUHCn24kEKj0ubb/GQebGqQjqOPDhTr/hICxbQ=
      access-token-expiration: 15m
      refresh-token-expiration: 30d

    email-verification:
      expiration: 24h

    password-reset:
      expiration: 30m

    account-lock:
      max-attempts: 5
      duration: 15m

auth:
  cleanup:
    revoked-session-retention: 30d
    verified-email-token-retention: 30d
    used-password-reset-token-retention: 30d

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/streaming_auth
    username: streaming
    password: streaming123

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
