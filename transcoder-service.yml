server:
  port: 8087

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: false
    instance-id: TRANSCODER-SERVICE:8087

  client:
    service-url:
      defaultZone: http://host.docker.internal:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info
